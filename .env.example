DATABASE_URL="file:./prisma/db/custom.db"
NEXTAUTH_SECRET="replace-with-a-long-random-string"
NEXTAUTH_URL="http://localhost:3000"
PORT=3000
XAI_API_KEY=
XAI_MODEL=grok-4.6

# Coolify: set NEXTAUTH_URL to the public HTTPS URL (e.g. https://sports.example.com)
# Coolify: mount a volume at /app/prisma/db so SQLite survives deploys
