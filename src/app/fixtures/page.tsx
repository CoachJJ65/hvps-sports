export default function FixturesPage() {
  return (
    <main className="px-4 pt-[max(1rem,env(safe-area-inset-top))]">
      <h1 className="pt-4 text-2xl font-semibold">Fixtures</h1>
      <p className="mt-2 text-sm text-muted-foreground">
        No fixtures published yet. This is the mobile-first shell — match data
        lands here next.
      </p>
    </main>
  );
}
